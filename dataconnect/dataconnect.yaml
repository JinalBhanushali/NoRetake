specVersion: "v1"
serviceId: "noretakes"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "noretakes-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
